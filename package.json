{
	"private": true,
	"scripts": {
		"start": "python3 -m http.server 1337",
		"format": "npx prettier --write ."
	},
	"dependencies": {
		"todomvc-app-css": "^2.0.0"
	},
	"devDependencies": {
		"@m-ld/m-ld": "^0.10.1-edge.0",
		"memory-level": "^1.0.0"
	},
	"dependenciesComments": {
		"todomvc-app-css": "TodoMVC boilerplate CSS (not in use, just copy/pasted into css/index.css)",
		"*": "Dev node module dependencies are loaded from CDN, see import map in index.html"
	}
}
