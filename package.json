{
	"private": true,
	"scripts": {
		"build": "esbuild jsx/app.jsx --bundle --format=esm --outfile=bundle.js --inject:./esbuild.shim.js",
		"dev": "npm run build -- --sourcemap --watch",
		"start": "python3 -m http.server 1337",
		"format": "npx prettier --write ."
	},
	"dependencies": {
		"@m-ld/m-ld": "^0.10.0",
		"buffer": "^6.0.3",
		"events": "^3.3.0",
		"immer": "^10.0.3",
		"memory-level": "^1.0.0",
		"react": "^18.2.0",
		"react-dom": "^18.2.0",
		"rxjs": "^7.8.1",
		"socket.io-client": "^4.7.2",
		"todomvc-app-css": "^2.0.0"
	},
	"devDependencies": {
		"@m-ld/m-ld": "^0.10.1-edge.0",
		"memory-level": "^1.0.0",
		"esbuild": "0.19.8"
	},
	"dependenciesComments": {
		"todomvc-app-css": "TodoMVC boilerplate CSS (not in use, just copy/pasted into css/index.css)",
		"*": "Dev node module dependencies are loaded from CDN, see import map in index.html"
	}
}
